# Client

React + Vite + TypeScript frontend. See the [root README](../README.md) for full setup, architecture, and API documentation.

```bash
npm install
npm run dev
```

Requires the backend (`../server`) running on `localhost:5000` — the dev server proxies `/api/*` automatically, no frontend `.env` needed.
